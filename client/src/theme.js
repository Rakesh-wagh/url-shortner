import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#9b4dca", // Purple color
    },
    secondary: {
      main: "#6a0dad", // Darker purple for contrast
    },
  },
});

export default theme;